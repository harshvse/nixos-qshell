Widget List

Bar modules

Workspaces indicator (active/occupied/empty states)
Active window title
Clock / date
System tray
CPU usage (%, graph, or sparkline)
Memory usage
Disk usage
Network (speed up/down, SSID, signal strength)
Battery (%, charging state, time remaining)
Volume (mute state, % slider on hover/click)
Microphone mute indicator
Bluetooth status
Media/now-playing (track, artist, play/pause/skip)
Idle inhibitor toggle

Popups / overlays

Volume OSD
Brightness OSD
Notification toast + notification center (history list)
Quick settings panel (wifi/bt/volume/brightness/night-light toggles combined)
Calendar popover (click clock → month view)
Power menu (lock/logout/suspend/reboot/shutdown)
App launcher (search + grid/list of apps)
Clipboard history picker
Emoji picker

Full-window components

Lockscreen (clock + password field + wallpaper)
Dashboard (media + weather + calendar + system stats combined, like a "today" screen)
Wallpaper picker/manager

Utility/system

Screenshot region picker UI (wraps grim/slurp)
Screen recording indicator
Weather widget (via API + Process/HTTP request)